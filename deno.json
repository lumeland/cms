{
  "lock": false,
  "tasks": {
    "dev": "deno run --unstable-kv --check --watch -A demo/proxy.ts",
    "update-deps": "deno run -A 'https://deno.land/x/nudd@v0.2.4/cli.ts' update deps/*.ts deno.json",
    "build-libs": "cd scripts && npm run build"
  },
  "exclude": [
    "probas",
    "static/libs",
    "CHANGELOG.md",
    "node_modules",
    "scripts/block-editor/dist"
  ]
}
